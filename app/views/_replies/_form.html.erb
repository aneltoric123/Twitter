<%= form_with(model: _reply) do |form| %>
  <% if _reply.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(_reply.errors.count, "error") %> prohibited this _reply from being saved:</h2>

      <ul>
        <% _reply.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
